// API base URL, environment flags, and feature toggles
const CONFIG = {
  API_BASE_URL: "http://localhost:8080/api",
  ENV: "development",
  FEATURES: {
    GLOBE_ENABLED: true,
    ANIMATIONS_ENABLED: true,
  },
};
